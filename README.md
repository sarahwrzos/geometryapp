# geometryapp

# npm install @svgdotjs/svg.js

# Ctrl+shift+i console

# to run locally: (write in terminal) npm run dev

to do:
create functions
    reset all
    delete line
    add diameter through point


figure out how to
    move the points and generate new line (show a list, manually change numbers or drag point)
    calculate angles
    only select points inside of circle
    only show inside of circle
    
# testing
npm install -D vitest
npm test

npm install mathjs